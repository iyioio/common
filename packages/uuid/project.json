{
    "name": "uuid",
    "$schema": "../../node_modules/nx/schemas/project-schema.json",
    "sourceRoot": "packages/uuid/src",
    "projectType": "library",
    "targets": {
        "build": {
            "executor": "@nrwl/js:tsc",
            "outputs": ["{options.outputPath}"],
            "options": {
                "outputPath": "dist/packages/uuid",
                "main": "packages/uuid/src/index.ts",
                "tsConfig": "packages/uuid/tsconfig.lib.json",
                "assets": ["packages/uuid/*.md"]
            }
        },
        "publish": {
            "executor": "nx:run-commands",
            "options": {
                "command": "node tools/scripts/publish.mjs uuid {args.ver} {args.tag}"
            },
            "dependsOn": [
                {
                    "projects": "self",
                    "target": "build"
                }
            ]
        },
        "lint": {
            "executor": "@nrwl/linter:eslint",
            "outputs": ["{options.outputFile}"],
            "options": {
                "lintFilePatterns": ["packages/uuid/**/*.ts"]
            }
        },
        "test": {
            "executor": "@nrwl/jest:jest",
            "outputs": ["coverage/packages/uuid"],
            "options": {
                "jestConfig": "packages/uuid/jest.config.ts",
                "passWithNoTests": true
            }
        }
    },
    "tags": []
}
